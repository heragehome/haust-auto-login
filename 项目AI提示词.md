# 校园网自动认证（大学掌 · POST 版）

零常驻 PowerShell 脚本，配合 Windows 任务计划程序实现校园网自动登录。

## 🎯 项目定位

本脚本是**河南科技大学（HAUST）新版校园网（大学掌 / autewifi.cn）的自动认证实现**，属于 POST 认证案例。

与本仓库 [v1.0](https://github.com/heragehome/haust-auto-login/releases/tag/v1.0)（锐捷 ePortal，GET 认证）组成**同一高校两代认证系统的对照案例**：

- **v1.0（旧，锐捷）＝ GET 认证案例**：透明代理劫持 + 302/200 探测 + `GET /eportal/portal/login` 表单 + JSONP 解析。
- **v2.0（本版，大学掌）＝ POST 认证案例**：自动发现网关 → 取 CSRF Token → `POST /api/account/login`（带 X-CSRF-Token 头）→ 状态接口复查。

> 对比两版即可理解 GET 表单认证 与 POST JSON API 认证的协议差异（请求头携带 vs URL 传参、CSRF 会话绑定、JSONP vs JSON），适合作为强制门户认证的逆向与二次开发参考。

**主要面向**：① 河科大 / 使用大学掌认证的河南高校学生（直接部署使用）；② 社区 / 开发者（协议逆向、迁移与二次开发案例）。

---

## 协议参考（本版为 POST）

**协议已于 2026-09-01 在校实测校准**（基于完整 HAR 抓包），四个框架期遗留问题全部确认：

| 框架期待确认项         | 实测结论                                                     |
| --------------- | -------------------------------------------------------- |
| CSRF Token 来源   | `GET /api/csrf-token` → `{"csrf_token":"..."}`，独立 API 接口 |
| 登录返回码           | `code=0` 成功 / `code=1` 失败（msg 说明原因）/ `code=2` 需验证码       |
| Cookie `yudear` | **非必需**。实测整个认证流程无任何 Cookie 下发与回传，登录照样成功                  |
| IP/MAC/vlan 参数  | 网关 302 重定向 URL 自动携带，脚本自动提取，无需手动配置                        |

## 工作原理（实测协议）

```
探测 http://www.msftconnecttest.com/connecttest.txt
  ├─ HTTP 200/204 ──────────────────────→ 已联网，退出
  └─ HTTP 302 → 10.100.51.1（网关 NetEngine 拦截）→ 未认证
        │
        ├─ 跟随重定向链，从 URL 提取 ip / mac / nasId / vlan
        │    /api/r/1?mac=..&vlan=..                    （网关附 MAC/VLAN）
        │    → /tpl/<皮肤>/login.html?ip=..&mac=..&nasId=..&vlan=..（附 IP/nasId）
        │
        ├─ GET  /api/csrf-token          → {"csrf_token":"..."}
        ├─ GET  /api/account/status      → code=1 不在线 → 继续
        │
        ├─ POST /api/account/login       → code=0 认证成功
        │    （X-CSRF-Token 头 + 表单体，密码明文）
        │    ★ CSRF token 绑定 TCP 连接：以上请求全程共用
        │      同一条 keep-alive 连接（浏览器同此行为）
        │
        └─ GET  /api/account/status 复查 → code=0 已在线，退出
```

**零占用设计**：脚本只在触发时运行，执行完毕立即退出，不驻留内存。触发方式靠 Windows 任务计划程序。

## 实测协议细节（2026-09-01）

### 认证服务器

| 项目      | 值                                                                                                                     |
| ------- | --------------------------------------------------------------------------------------------------------------------- |
| 认证服务器   | `http://10.100.51.1`（nginx/1.21.5）                                                                                    |
| 登录入口模板  | `/tpl/stu-xy/login.html` → JS 跳转 `login_account.html`（皮肤名可能变化，脚本自动跟随重定向，不硬编码）                                         |
| CSRF 接口 | `GET /api/csrf-token` → `{"csrf_token":"..."}`                                                                        |
| 状态接口    | `GET /api/account/status?username=&password=&switchip=&nasId=&userIpv4=&userMac=&captcha=&captchaId=&isp=&timeLimit=` |
| 登录接口    | `POST /api/account/login`                                                                                             |

### 登录 POST

请求头（模拟浏览器 AJAX）：

```
Content-Type: application/x-www-form-urlencoded; charset=UTF-8
Origin: http://10.100.51.1
Referer: http://10.100.51.1/tpl/<皮肤>/login_account.html?ip=..&mac=..&nasId=..&vlan=..
X-CSRF-Token: <csrf-token 接口返回值>
X-Requested-With: XMLHttpRequest
```

请求体（字段顺序与浏览器一致，`userMac` 中冒号按 URL 编码为 `%3A`）：

```
username=学号&password=密码&nasId=1&userIpv4=内网IP&userMac=e0%3Ac2%3A...&isp=local&timeLimit=
```

返回码语义（源自登录页 JS 源码 `login_account.html`）：

| code | 含义                                                 | 处理                      |
| ---- | -------------------------------------------------- | ----------------------- |
| 0    | 认证成功                                               | 完成认证                    |
| 1    | 认证失败（`msg` 含原因："认证失败" / "第三方密码校验失败" / "第三方密码校验超时"） | 记录日志，重试                 |
| 2    | 需要验证码（返回 `captcha.picPath` / `captchaId`）          | **脚本无法自动处理**，需浏览器手动登录一次 |

### 自助门户（210.43.0.48）

认证成功后浏览器跳转 `http://210.43.0.48/#/home`（Vue 单页应用）。

| 接口                                          | 方法                         | 说明                                                            |
| ------------------------------------------- | -------------------------- | ------------------------------------------------------------- |
| `http://210.43.0.48/mac/?clientIP=`         | GET                        | 查询在线设备列表（`data.notOnline=false` 且 `deviceList[].online=true`） |
| `http://210.43.0.48/mac/loginOut?clientIP=` | POST，body `mac=<URL编码MAC>` | 下线本机（无 Cookie、无 CSRF）                                         |

脚本用状态接口 `/api/account/status` 做主验证（`code=0` 即在线），门户接口做下线功能。

### 无 Cookie 依赖

实测两次完整登录（含成功），**全程没有任何 Set-Cookie 响应头，也没有任何请求携带 Cookie 头**，登录照常成功。6 月抓包里出现的 `yudear` Cookie 是浏览器历史会话残留（其 base64 解码出的时间戳为 2026-06-22），与认证无关。

### 供应商线索

- 登录页皮肤前缀 `dxz-*`、`dxzBindApiUrl`/`dxzCampusCode` 等指向「大学掌」平台（autewifi.cn，小程序扫码/绑定体系）

- Cookie 名 `yudear`、网关标识 `NetEngine Server 1.0` 为认证后端/网关自身标识

- 模板目录 `stu-xy` 为学校自定义皮肤名

## 文件结构

```
campus-auth/
├── ① 运行DryRun测试.bat  # 双击运行：安全预演（不实际登录）
├── ② 立即认证.bat        # 双击运行：立即认证一次
├── ③ 安装任务计划.bat     # 双击运行：安装开机/断网自动认证（自动请求管理员）
├── ④ 下线本机.bat        # 双击运行：立即下线
├── ⑤ 解除锁定（只需运行一次）.bat  # 首次运行：去除下载标记，免授权弹窗
├── config.json           # 配置文件（账号密码均为占位符，下载后请自行填写）
├── CampusAuth.ps1        # 主脚本 v1.0.4（UTF-8 带 BOM，兼容 PowerShell 5.1）
├── Install-Task.ps1      # 安装任务计划（三个触发器全自动注册）
├── Uninstall-Task.ps1    # 卸载任务计划
└── README.md             # 本文件
```

> .ps1 文件均带 BOM 头，中文 Windows 的 PowerShell 5.1 才能正确解码；
> .bat 启动器自带 `-ExecutionPolicy Bypass` 与 `pause`，双击即可运行且出错不闪退。

## 快速使用（全程双击，无需命令行）

> **首次使用按 ⑤→①→②→③ 的顺序**：先解除锁定（免授权弹窗），再 DryRun 预演核对，再手动认证，最后装任务计划。

1. **双击** **`⑤ 解除锁定（只需运行一次）.bat`** —— 清除 Windows 给下载文件打的"网络来源"标记。
   从此双击任何 .bat/.ps1 都不会再弹"打开文件 - 安全警告"（本次运行 ⑤ 本身还会弹最后一次，点"运行"）。
2. **双击** **`① 运行DryRun测试.bat`** —— 只探测网络、取参数、读接口，**不实际登录**。
   窗口会停住显示每一步结果，正常情况看到"提取到认证参数"即可；把窗口截图发给助手核对。
3. **双击** **`② 立即认证.bat`** —— 真实登录一次。看到"认证成功，已联网"即全部就绪。
4. **双击** **`③ 安装任务计划.bat`** —— 安装自动认证（会弹管理员确认框，点"是"）。
   之后开机登录 / 断网恢复 / 每 10 分钟，都会自动检查并按需登录，零常驻。
5. 想下线：**双击** **`④ 下线本机.bat`**。

所有窗口执行完都会停住（`pause`），出错时可直接截图，不会再"黑框一闪而过"。

### 命令行方式（进阶，可选）

```powershell
.\CampusAuth.ps1 -DryRun        # 模拟运行
.\CampusAuth.ps1                # 正常认证
.\CampusAuth.ps1 -Logout        # 下线本机
.\Install-Task.ps1              # 安装任务计划（需管理员）
.\Uninstall-Task.ps1            # 卸载
```

## 故障排查

**症状：每次运行 .bat/.ps1 都弹"打开文件 - 安全警告"**
→ 下载的文件带"网络来源"标记（MOTW）。双击 `⑤ 解除锁定（只需运行一次）.bat` 即可永久解除；
也可在解压前右键压缩包 → 属性 → 勾选"解除锁定" → 确定，再解压。

**症状：报错** **`Write-Log : 找不到接受实际参数"ERROR"的位置形式参数`**
→ v1.0.1 及之前的 bug：Write-Log 的 Level 参数缺 Position 声明，导致按位置传参失败。
v1.0.2 已修复，更新脚本即可。

**症状：报错** **`config.json 没有找到！`**
→ v1.0.2 起会自动尝试 4 个候选路径，并在窗口列出脚本目录的实际文件清单。
按窗口提示截图发回即可定位；多数情况是解压不完整，重新解压整个压缩包即可。

**症状：报错** **`找不到类型 [System.Net.Http.HttpClientHandler]`**
→ PowerShell 5.1 不自动加载该程序集。v1.0.3 起脚本启动时显式 `Add-Type -AssemblyName System.Net.Http`，更新脚本即可。

**症状：`登录失败: code=, msg=`（code 和 msg 都是空）**
→ v1.0.3 及之前的致命 bug：认证服务器的 **CSRF token 绑定 TCP 连接**，脚本每个请求新建 HttpClient（新连接），
服务器返回 `HTTP 400 {"error":"CSRF token mismatch"}`（无 code/msg 字段所以显示为空）。
v1.0.4 起全程共用一个 HttpClient，实测已修复；若再出现，日志会原样输出服务器响应便于定位。

**症状：报错** **`Rename-Item : 无法重命名指定的目标，因为该目标表示路径或设备名称`**
→ v1.0.2 及之前日志轮转的 NewName 带了路径 + 配置未加载阶段轮转阈值判空误触发。v1.0.3 已修复。

**症状：黑框一闪而过，没有生成 log**
→ v1.0.0 的问题（无 BOM 中文脚本解析失败 / 执行策略拦截），v1.0.1 起已修复。
请一律使用 .bat 启动器运行（策略豁免 + 窗口停住）。

## 触发时机

| 触发器   | 说明                                      | 状态         |
| ----- | --------------------------------------- | ---------- |
| 用户登录时 | 开机登录后自动触发                               | ✓ 自动安装     |
| 定时轮询  | 每 10 分钟检查一次（兜底）                         | ✓ 自动安装     |
| 网络变更  | 切换 WiFi / 插拔网线（NetworkProfile 事件 10000） | ✓ 自动注入 XML |

## 已知限制

- **验证码（code=2）无法自动化**：连续输错密码后服务器会要求图形验证码，此时请用浏览器打开认证页手动登录一次，之后脚本恢复正常

- **密码明文**：认证协议本身即明文传输（HTTP 无加密层），config.json 中的密码同样为明文，请勿将本文件夹提交到公开仓库

- **IPv6 未验证**：本协议只覆盖 IPv4 认证，校园网 IPv6 直通特性未测试

- **皮肤名变化**：学校可能更换登录页皮肤（实测 6 月为 `default`、9 月为 `stu-xy`），脚本不硬编码，自动跟随重定向

## 日志

日志文件默认在脚本同目录下的 `campus-auth.log`，超过 512KB 自动轮转为 `.old`。

```
[2026-09-01 09:45:00] [INFO] ========== 校园网自动认证开始 ==========
[2026-09-01 09:45:00] [WARN] 网络未认证（重定向到 10.100.51.1）
[2026-09-01 09:45:01] [INFO] 提取到认证参数: ip=10.100.0.100, mac=00:11:22:33:44:55, nasId=1, vlan=ethtrunk/1:3002.3752
[2026-09-01 09:45:01] [INFO] 登录尝试 1 / 3
[2026-09-01 09:45:02] [INFO] 登录成功！（服务器返回 code=0）
[2026-09-01 09:45:04] [INFO] 账户已在线: 202600000000
[2026-09-01 09:45:04] [INFO] ========== 认证成功，已联网 ==========
```

## 版本历史

| 版本    | 日期         | 说明                                                                                                                                                                                                                                                |
| ----- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0.1.0 | 2026-06    | 框架版，按 6 月抓包搭建，待到校完善                                                                                                                                                                                                                               |
| 1.0.0 | 2026-09-01 | 协议实测校准版：CSRF 来源确认、返回码确认、去除 Cookie 依赖、新增下线功能、任务计划全自动注册                                                                                                                                                                                             |
| 1.0.1 | 2026-09-01 | 修复"黑框闪退无日志"：.ps1 补 UTF-8 BOM（PowerShell 5.1 中文兼容）、新增 4 个双击即用的 .bat 启动器（策略豁免 + 窗口驻留可截图）                                                                                                                                                            |
| 1.0.2 | 2026-09-01 | 修复 Write-Log 位置参数绑定 bug；Load-Config 支持多候选路径 + 目录清单自诊断；新增 ⑤ 解除锁定.bat（去除下载文件 MOTW 标记，免授权弹窗）；DryRun 窗口显示文件夹清单                                                                                                                                        |
| 1.0.3 | 2026-09-01 | 首次实机运行反馈修复：显式加载 System.Net.Http 程序集（PS 5.1 不自动加载，此前为致命阻塞）；修复日志轮转误触发与 Rename-Item 路径 bug；param() 默认值不再依赖 $PSScriptRoot（PS 5.1 下未就绪），配置与脚本路径改为脚本体内自动发现                                                                                              |
| 1.0.4 | 2026-09-01 | **修复"登录失败: code=, msg="空报错**：实测发现认证服务器的 CSRF token 与 TCP 连接绑定，取 token 与登录必须走同一条 keep-alive 连接，否则返回 `{"error":"CSRF token mismatch"}`。v1.0.3 每个请求新建 HttpClient（新连接）导致必然失败。现改为全程复用同一个共享 HttpClient。已通过"下线→自动认证→恢复联网"完整闭环实测验证。同时改进失败日志（原始响应体现在会原样输出） |

## 备注

- 脚本使用 `System.Net.Http.HttpClient` 手动控制重定向与请求头，兼容 PowerShell 5.1（Windows 10+ 自带）

- 认证逻辑参考登录页自带 JS 源码（`login_account.html` 内嵌逻辑 + `csrf.js`），与浏览器行为逐字段一致

